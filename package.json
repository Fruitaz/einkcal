{
  "name": "einkcal-screenshot-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node api/screenshot.js",
    "postinstall": "puppeteer install"
  },
  "dependencies": {
    "@sparticuz/chromium": "^114.0.0",
    "puppeteer-core": "^23.3.0",  // Use puppeteer-core as it avoids downloading its own Chromium
    "ftp": "^0.3.10"
  }
}
